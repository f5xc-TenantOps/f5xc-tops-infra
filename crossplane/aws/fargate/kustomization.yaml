apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster.yaml
  - task-definition.yaml
  - iam.yaml
  - log-group.yaml
  - scheduled-rules.yaml

# Account ID replacement handled via ArgoCD ApplicationSet or Crossplane composition
# Configure AWS_ACCOUNT_ID in ArgoCD or use sed replacement in CI
